name: Build Vite and deploy to Hostinger

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: "mi-landing/package-lock.json"

      - name: Install dependencies
        run: npm ci --no-audit --no-fund
        working-directory: mi-landing

      - name: Build Vite
        run: |
          npm run build
          test -f public/build/manifest.json
        working-directory: mi-landing

      - name: Ensure remote build dir exists
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOSTINGER_HOST }}
          username: ${{ secrets.HOSTINGER_USER }}
          key: ${{ secrets.HOSTINGER_SSH_KEY }}
          port: ${{ secrets.HOSTINGER_PORT }}
          script: |
            mkdir -p "${{ secrets.HOSTINGER_APP_PATH }}/public/build"

      - name: Upload Vite build to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.HOSTINGER_HOST }}
          username: ${{ secrets.HOSTINGER_USER }}
          key: ${{ secrets.HOSTINGER_SSH_KEY }}
          port: ${{ secrets.HOSTINGER_PORT }}
          source: "mi-landing/public/build/*"
          target: "${{ secrets.HOSTINGER_APP_PATH }}/public/build"
          strip_components: 3

      - name: Clear Laravel caches
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOSTINGER_HOST }}
          username: ${{ secrets.HOSTINGER_USER }}
          key: ${{ secrets.HOSTINGER_SSH_KEY }}
          port: ${{ secrets.HOSTINGER_PORT }}
          script: |
            cd "${{ secrets.HOSTINGER_APP_PATH }}"
            php artisan optimize:clear
            ls -la public/build/manifest.json
